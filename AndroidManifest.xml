<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="paulino.calderon.android.bcbus" android:installLocation="preferExternal" 
      android:versionCode="1"
      android:versionName="1.0 alpha">
    <application android:icon="@drawable/icon" android:label="@string/app_name"  android:theme="@style/customTheme">
        <activity android:name=".HomeActivity" 
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            
            
        </activity>


                    <activity
            android:name=".ScheduleActivity"
            android:label="@string/title_schedule"
            android:configChanges="orientation|keyboardHidden" />
             <activity
            android:name=".DisplayScheduleActivity"
            android:label="@string/title_display_schedule"
            android:configChanges="orientation|keyboardHidden" />
            <activity
            android:name=".PreferencesActivity"
            android:label="@string/title_preferences"
            android:configChanges="orientation|keyboardHidden" />
            <activity
            android:name=".DisplayBusStopActivity"
            android:label="@string/title_display_stop"
            android:configChanges="orientation|keyboardHidden" />
    </application>
    <uses-sdk android:minSdkVersion="3" />
<uses-permission android:name="android.permission.INTERNET"></uses-permission>
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
</manifest> 